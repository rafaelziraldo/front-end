
import { types } from '../types/types';


export const verTabla = () => ({ type: types.verTabla });
export const verCreate = () => ({ type: types.verCreate });
export const verEdit = () => ({ type: types.verEdit });